[env]
# Where ts-rs writes the TypeScript side of every type that crosses the IPC
# boundary. Regenerated by `npm run types` (which runs `cargo test
# export_bindings`); the output is committed so the frontend builds without
# a Rust toolchain.
TS_RS_EXPORT_DIR = { value = "../src/generated", relative = true }
